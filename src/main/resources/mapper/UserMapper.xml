<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.teachingplatform.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.teachingplatform.entity.User">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,username,password,
        role,create_time
    </sql>
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into user(username, password, role, create_time)
        values (#{username}, #{password}, #{role}, #{createTime})
    </insert>

    <update id="update">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="getById" resultMap="BaseResultMap">
        select id,
               username,
               password,
               role,
               create_time
        from user
        where id = #{id}
    </select>
    <select id="login" resultMap="BaseResultMap">
        select id,
               username,
               password,
               role,
               create_time
        from user
        where username = #{username} and password = #{password}
    </select>
</mapper>
